/**
 * Contratos de persistência consumidos pela aplicação e implementados por
 * adapters de infrastructure. Reutilizam tipos existentes, sem importar UI,
 * implementações de storage ou APIs do navegador.
 */
export type { CycleRepository } from "./CycleRepository";
export type { CollaboratorRepository } from "./CollaboratorRepository";
