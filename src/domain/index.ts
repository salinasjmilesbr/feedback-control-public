/**
 * Fronteira para entidades, invariantes e cálculos puros do domínio.
 * Não depende de UI, application, infrastructure, serviços de persistência
 * ou APIs do navegador. Tipos puros de src/types podem ser reutilizados.
 * As regras existentes permanecem em seus arquivos atuais durante a transição;
 * sua adoção nesta camada deve ser incremental, sem duplicação de regras.
 */
export {};
